{% extends 'base.html.twig' %}

{% block title %}Créer{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Ajouter une histoire</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary w-100 mt-5">
            <i class="fas fa-check"></i>
            Créer
        </button>
        {{ form_end(form) }}
    </div>

{% endblock %}
